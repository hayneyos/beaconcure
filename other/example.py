# Add the handler to the application's logger


# "encode(image, “This is an example text”)"
# Decipher encoded_git_result.png
# bot.handle_file_shared("encoded_git_result.png")
# bot.client.chat_postMessage(channel="test", text="Check2")
# encode("encoded_git_result.png", “This is an example text”)
